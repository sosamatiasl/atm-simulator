<Window x:Class="ATM_Simulator_WPF.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="ATM Operaciones" Height="450" Width="600"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Cuenta.NumeroCuenta, StringFormat='Operando con Cuenta: {0}'}" 
                   FontSize="16" FontWeight="SemiBold" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Saldo Actual: " FontWeight="Bold"/>
            <TextBlock Text="{Binding Saldo, StringFormat='{}{0:C}'}"/>
        </StackPanel>

        <StackPanel Grid.Row="2">
            <TextBlock Text="Monto de la Transacción:" Margin="0,0,0,5"/>
            <TextBox Text="{Binding MontoTransaccion, UpdateSourceTrigger=PropertyChanged}" 
                     Height="30" Width="200" HorizontalAlignment="Left" Margin="0,0,0,20"/>

            <StackPanel Orientation="Horizontal">
                <Button Content="Retirar" Command="{Binding RetirarCommand}" Width="100" Margin="0,0,10,0" Padding="5"/>
                <Button Content="Depositar" Command="{Binding DepositarCommand}" Width="100" Margin="0,0,10,0" Padding="5"/>
                <Button Content="Consultar Saldo" Command="{Binding ConsultarSaldoCommand}" Width="120" Padding="5"/>
            </StackPanel>

            <TextBlock Text="{Binding MensajeOperacion}" FontSize="14" Margin="0,30,0,0" Foreground="Blue"/>
        </StackPanel>

    </Grid>
</Window>