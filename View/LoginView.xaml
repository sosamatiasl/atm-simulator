<Window x:Class="ATM_Simulator_WPF.View.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ATM_Simulator_WPF.View"
        xmlns:vm="clr-namespace:ATM_Simulator_WPF.ViewModel"
        mc:Ignorable="d"
        Title="ATM Login" Height="350" Width="400"
        WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <vm:LoginViewModel/>
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Simulador de Cajero Automático" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Margin="0,10">
            <TextBlock Text="Número de Cuenta (16 dígitos):"/>
            <TextBox Text="{Binding NumeroCuenta, UpdateSourceTrigger=PropertyChanged}" MaxLength="16" Margin="0,5,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,10">
            <TextBlock Text="PIN (4 dígitos):"/>
            <TextBox Text="{Binding PIN, UpdateSourceTrigger=PropertyChanged}" MaxLength="4" Margin="0,5,0,0"/>
        </StackPanel>

        <Button Grid.Row="3" Content="Ingresar" Command="{Binding LoginCommand}" Margin="0,20,0,10" Padding="10"/>

        <TextBlock Grid.Row="4" Text="{Binding MensajeError}" Foreground="Red" HorizontalAlignment="Center" Margin="0,10,0,0"/>
    </Grid>
</Window>